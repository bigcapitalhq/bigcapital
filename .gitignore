node_modules/

# Docker volumes data directory
/data

# Env files (.env, .env.production, .env.bak, etc.) — secrets, never commit.
# An example template (.env.example) would be the only tracked variant.
.env
.env.*
!.env.example

# Local-dev compose override (port remaps, MinIO for attachments, etc.)
docker-compose.override.yml

# Ad-hoc creds scratchpads — never commit
*-creds.txt
gather-square-creds.txt

# Local working / archive directory (Amazon import scripts, scans, etc.)
archive/

# Throwaway ESLint config used for one-off jsx-key scans
.eslintrc.scan.cjs

test-results/
.qodo


.pnpm-store

# Built image tarballs, organized by target environment
current-images/
*.tar.gz

# Per-environment deploy config (DEPLOY_HOST, etc.) — host secrets, never commit
docker/*/.deploy.conf

# macOS Finder metadata
.DS_Store

# Log files
*.log

# Local scratch notes (sandbox creds, curl recipes, ad-hoc reminders)
*-notes.txt

# Square data pulls (real seller data, never commit)
square-import-sample/
square-import-sample.zip

# Tenant DB snapshots (real data, large, never commit)
*-pre-square-import-*.sql
bigcapital_tenant_*.sql