{
  "restartable": "rs",
  "watch": [
    "src",
    ".env"
  ],
  "ignore": [
    ".git",
    "src/**/*.spec.ts"
  ],
  "execMap": {
    "ts": "node --inspect -r ts-node/register/transpile-only ./src/server.ts"
  },
  "env": {
    "NODE_ENV": "development"
  }
}