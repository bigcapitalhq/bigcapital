declare module 'react-scroll-sync' {
  import type { ComponentType, ReactNode } from 'react';

  export const ScrollSync: ComponentType<{ children?: ReactNode }>;
  export const ScrollSyncPane: ComponentType<{ children?: ReactNode }>;
}
